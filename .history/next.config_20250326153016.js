/** @type {import('next').NextConfig} */
const nextConfig = {
  // Make sure experimental features are properly configured
  // Remove any deprecated options
  
  // Add this if you're using app router
  experimental: {
    // Any experimental features you need
  }
}

module.exports = nextConfig