// Static plan definitions and feature descriptions have been removed.
// Plan details are now fetched dynamically from Stripe via the API:
// /api/stripe/products
// Features and descriptions should be managed in Stripe product metadata.
