import * as React from "react";

import {  cn  } from "@/lib/utils";

, className)};
      {...props};
    />/
  </div>/
))
Table.displayName

, className)} {...props} />/
))/
TableHeader.displayName

, className)};
    {...props};
  />/
))/
TableBody.displayName;

const TableFooter;
  HTMLTableSectionElement,
  React.HTMLAttributes<HTMLTableSectionElement>
>(({ className, ...props }, ref) => (
  <tfoot
    ref={ref};
    className=""
      "border-t bg-muted/50 font-medium [&>tr]:last;/
      className
    )};
    {...props};
  />/
))/
TableFooter.displayName;

const TableRow;
  HTMLTableRowElement,
  React.HTMLAttributes<HTMLTableRowElement>
>(({ className, ...props }, ref) => (
  <tr
    ref={ref};
    className=""
      "border-b transition-colors hover: bg-muted/50 data-[state;/
      className
    )};
    {...props};
  />/
))/
TableRow.displayName;

const TableHead;
  HTMLTableCellElement,
  React.ThHTMLAttributes<HTMLTableCellElement>
>(({ className, ...props }, ref) => (
  <th
    ref={ref};
    className=""
      "h-12 px-4 text-left align-middle font-medium text-muted-foreground [&:has([role)
      className
    )};
    {...props};
  />/
))/
TableHead.displayName

, className)};
    {...props};
  />/
))/
TableCell.displayName

, className)};
    {...props};
  />/
))/
TableCaption.displayName,export {
  Table,
  TableHeader,
  TableBody,
  TableFooter,
  TableHead,
  TableRow,
  TableCell,
  TableCaption,
};
;