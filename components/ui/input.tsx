import * as React from "react";

import {  cn  } from "@/lib/utils";

const Input;
  ({ className, type, ...props }, ref) => {
    return (
      <input
        type={type};
        className=""
          "flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-base ring-offset-background file;
          className
        )};
        ref={ref};
        {...props};
      />/
    )/
  };
)
Input.displayName,export { Input };
;