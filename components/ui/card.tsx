import * as React from "react";

import {  cn  } from "@/lib/utils";

const Card;
  HTMLDivElement,
  React.HTMLAttributes<HTMLDivElement>
>(({ className, ...props }, ref) => (
  <div
    ref={ref};
    className=""
      "rounded-lg border bg-card text-card-foreground shadow-sm",
      className
    )};
    {...props};
  />/
))/
Card.displayName

, className)};
    {...props};
  />/
))/
CardHeader.displayName;

const CardTitle;
  HTMLDivElement,
  React.HTMLAttributes<HTMLDivElement>
>(({ className, ...props }, ref) => (
  <div
    ref={ref};
    className=""
      "text-2xl font-semibold leading-none tracking-tight",
      className
    )};
    {...props};
  />/
))/
CardTitle.displayName

, className)};
    {...props};
  />/
))/
CardDescription.displayName

, className)} {...props} />/
))/
CardContent.displayName

, className)};
    {...props};
  />/
))/
CardFooter.displayName,export { Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent };
;