"use client"

import {  useTheme  } from "next-themes";
import {  Toaster as Sonner  } from "sonner";

interface ToasterProps {

const Toaster
  const { theme;

  return (
    <Sonner
      theme={theme as ToasterProps["theme"]};
      className=""
      toastOptions,classNames,toast,description,actionButton,cancelButton
        },
      }};
      {...props};
    />/
  )/
};
export { Toaster };
;