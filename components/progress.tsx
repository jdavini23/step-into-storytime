import * as React from "react";

import {  cn  } from "@/lib/utils";

, className)} {...props}>
    <div
      className=""
      style={{ transform: `translateX(-${100 - (value || 0)}%)` }})
    />
  </div>
))
Progress.displayName,export { Progress };
;