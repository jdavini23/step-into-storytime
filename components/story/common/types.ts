// Placeholder types - please update with actual definitions

export interface StoryParagraph {
  // Define StoryParagraph properties here
  id: string;
  text: string;
  // Add other relevant properties
}

export interface ThemeColors {
  // Define ThemeColors properties here
  primary: string;
  secondary: string;
  background: string;
  text: string;
  // Add other relevant properties
}

export type FontSize = 'small' | 'medium' | 'large' | 'xlarge';

// Add any other shared types needed for story components here
